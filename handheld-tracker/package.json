{
  "name": "handheld-tracker",
  "private": true,
  "version": "1.0.0",
  "description": "Firecrawl-backed retro handheld price/value tracker (backend + Vite frontend).",
  "scripts": {
    "setup": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && node seedMock.js 30",
    "dev": "concurrently -n api,web -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "cd backend && npm start",
    "dev:web": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
